worker_processes 1;

events {
	worker_connections 1024;
}

http {
	include mime.types;
	default_type application/octet-stream;
	
	gzip on;
	server_tokens off;
	
	client_max_body_size 512M;
	
	include conf.d/*.conf;
}
